let a = 1, b = 1;
let c = ++a; // 2
let d = b++; // 1
alert(c);// 2, a pasa a valer 2 también por que añadimos uno más
alert(d);//1 por que "b" pasa a valer luego.
alert(a);// es 2 como dijimos antes.
alert(b);//2 por que ya fué incrementado.